name: Deploy Static Site

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Verify files exist
      run: |
        echo "Kontrollerar att nödvändiga filer finns..."
        ls -la
        test -f index.html || (echo "index.html saknas!" && exit 1)
        echo "✅ Alla nödvändiga filer finns"
        
    - name: Test HTML validity (basic)
      run: |
        echo "Grundläggande HTML-validering..."
        if grep -q "<html" index.html && grep -q "</html>" index.html; then
          echo "✅ HTML ser korrekt ut"
        else
          echo "❌ HTML-struktur verkar felaktig"
          exit 1
        fi
        
    - name: Simulate Deployment
      if: github.ref == 'refs/heads/main'
      run: |
        echo "🚀 Deploying website..."
        echo "📁 Files validated and ready"
        echo "🌐 Deployment URL: https://www.saucedemo.com"
        echo "✅ Deployment completed successfully!"
        
    - name: Trigger E2E Tests
      if: github.ref == 'refs/heads/main'
      uses: peter-evans/repository-dispatch@v3
      with:
        token: ${{ secrets.DISPATCH_TOKEN }}
        repository: ${{ secrets.TEST_REPO }}
        event-type: deployment-completed
        client-payload: |
          {
            "deployed_url": "https://www.saucedemo.com",
            "commit_sha": "${{ github.sha }}",
            "commit_message": "${{ github.event.head_commit.message }}",
            "repository": "${{ github.repository }}",
            "deployment_type": "simulated"
          }
